# ── Core ML / GPU ─────────────────────────────────────────────
torch==2.2.0
torchvision==0.17.0
torchaudio==2.2.0

# ── Transformers ecosystem ─────────────────────────────────────
transformers==4.48.0
accelerate==1.3.0
peft==0.14.0
sentence-transformers==3.3.1
sentencepiece
sacremoses

# ── HuggingFace utilities ──────────────────────────────────────
datasets==3.2.0
huggingface_hub==0.27.1

# ── Retrieval ──────────────────────────────────────────────────
faiss-cpu
rank-bm25

# ── Multilingual support (our extension) ──────────────────────
langdetect                   # automatic language detection
# Helsinki-NLP MarianMT models are loaded at runtime via HuggingFace Hub

# ── NLP utilities ─────────────────────────────────────────────
nltk

# ── Data / compute ────────────────────────────────────────────
numpy==1.26.4
pandas
scikit-learn

# ── General utilities ─────────────────────────────────────────
PyYAML
requests
tqdm

# ── Optional: 8-bit quantisation (for low-VRAM GPUs) ──────────
bitsandbytes==0.43.0

# ── FlashRAG (install separately from source) ─────────────────
# git clone https://github.com/RUC-NLPIR/FlashRAG.git
# cd FlashRAG && pip install -e .
levenshtein                  # required by FlashRAG
